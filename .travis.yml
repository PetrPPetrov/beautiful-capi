language: cpp

compiler:
  - gcc

script:
  - cmake -G "Unix Makefiles" && make

os:
  - linux

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake
