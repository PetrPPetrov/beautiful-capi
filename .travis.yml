sudo: required
language: cpp
dist: trusty

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libboost1.56-all-dev

compiler:
  - gcc

script:
  - cmake --version && python3 --version && cmake -G "Unix Makefiles" && make

os:
  - linux
